from Logic.py import Block 


while(1):
    state = 0

    if state == 0 :
        #initialise 
        
        #find blocks (not sure how to do this should be in max's code)

        #create block classes 
        blue = Block(x, y, z)
        green = Block(x, y, z)
        red = Block(x, y, z)
        yellow = Block(x, y, z)

                

    if state == 1 :
        #choose what block to pick up 


    if state == 2 :
        #Grab and move 


    if state == 3 :
        #reset 
        
